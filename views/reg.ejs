<!DOCTYPE html>

<form class="form-horizontal" method="post" action="/register">
	<fieldset>
		<legend>註冊</legend>

		<% if(typeof error2 != 'undefined'){ %>
		<p><font color="red"> <%= error2 %> </font></p>	
		<% } %>

		<div class="control-group">
			<label class="control-label" for="account">使用者帳戶</label>
			<div class="controls">
				<input type="text" class="input-xlarge" id="account" name="account">
				<p class="help-block">你的帳戶的名稱，用於登入。</p>
			</div>
		</div>
		<div class="control-group">
			<label class="control-label" for="name">使用者暱稱</label>
			<div class="controls">
				<input type="text" class="input-xlarge" id="name" name="name">
				<p class="help-block">你的暱稱，用於顯示。</p>
			</div>
		</div>
		<div class="control-group">
			<label class="control-label" for="password">密碼</label>
			<div class="controls">
				<input type="password" class="input-xlarge" id="password" name="password">
			</div>
		</div>
		<div class="control-group">
			<label class="control-label" for="password2">重覆密碼</label>
			<div class="controls">
				<input type="password" class="input-xlarge" id="password2" name="password2">
			</div>
		</div>
		<div class="form-actions">
			<button type="submit" class="btn btn-primary">註冊</button>
		</div>
	</fieldset>
</form>
